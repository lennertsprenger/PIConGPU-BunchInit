# install with:
# micromamba env create --file picongpu-bunchInit.yml

name: picongpu-bunchInit
channels:
  - defaults
  - conda-forge

dependencies:
  - scipy >= 1.7.1
  - ipython
  - jupyterlab
  - jupyter_contrib_nbextensions
  - matplotlib
  - openpmd-api >=0.15.2
  - numba 
  - cuda-toolkit